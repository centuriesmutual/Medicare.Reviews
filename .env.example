# Local SQLite (default). For PostgreSQL, set provider to postgresql in prisma/schema.prisma
# and use a connection string like: postgresql://USER:PASSWORD@localhost:5432/medicare_reviews
DATABASE_URL="file:./dev.db"
